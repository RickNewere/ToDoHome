<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds may point the WebView at the Vite dev server over plain HTTP
     on the local network, which release builds must never do. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application android:usesCleartextTraffic="true">

        <!-- The daily reminder fires at nine in the morning, which is no use for
             checking that it works. Exporting the receiver in debug builds lets
             adb set it off on demand:

             adb shell am broadcast -n it.ricknewere.todohome/.notify.ReminderReceiver \
               -a it.ricknewere.todohome.ACTION_CHECK

             Release builds keep it closed. -->
        <receiver
            android:name=".notify.ReminderReceiver"
            android:exported="true"
            tools:replace="android:exported" />

    </application>

</manifest>
