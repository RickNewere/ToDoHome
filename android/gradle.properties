org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# URL loaded by the WebView. Point it at the GitHub Pages deploy, or at the Vite
# dev server (http://<ip-del-pc>:5173/ToDoHome/) while testing on the phone.
# Override locally in local.properties, which is not committed.
TODOHOME_URL=https://ricknewere.github.io/ToDoHome/
